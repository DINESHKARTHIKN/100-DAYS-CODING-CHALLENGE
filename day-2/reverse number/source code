#include <stdio.h>
int main() {
    int n,rem,r,temp;
    scanf("%d",&n);
    temp=n;
    while(n!=0){
        rem=n%10;
        r=r*10+rem;
        n/=10;
    }
    printf("The reversed number of %d is %d",temp,r);
    return 0;
}
